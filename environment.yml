name: capstone
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - scipy
  - matplotlib
  # - torch
  - ffmpeg

  - pip:
      - openai-whisper
      - praat-parselmouth
      - opensmile
      - audiofile
      - pyaudio
      - webrtcvad
      - watchdog
      - streamlit
      - pandas
      - spacy
      - transformers
      - torch
      # FerbAI dashboard API
      - fastapi
      - "uvicorn[standard]"
      - scikit-learn
      - anthropic
      - nltk
